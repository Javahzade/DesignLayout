import React from 'react';
import {StyleSheet, View} from 'react-native';
import {C} from './C';

export const B = ({num}) => {
  return <C num={num} />;
};

const styles = StyleSheet.create({
  container: {},
});
