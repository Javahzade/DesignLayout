import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

export const D = ({num}) => {
  return <Text style={{fontSize: 48}}>{num}</Text>;
};

const styles = StyleSheet.create({
  container: {},
});
