import React from 'react';
import {StyleSheet, View} from 'react-native';
import {D} from './D';

export const C = ({num}) => {
  return <D num={num} />;
};

const styles = StyleSheet.create({
  container: {},
});
